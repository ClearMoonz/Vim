#include<project.h>
#include<stdio.h>
void readFile(const char *filePath,char *buffer){
	FILE*fp=fopen(filePath,"r");
	if(filePath==NULL){
	    printf("file doesn't exist\n");
	}else {
        
	}	
}		
